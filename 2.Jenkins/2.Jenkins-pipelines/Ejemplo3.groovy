pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // Pasos a Ejecutar
            }
        }
        stage('Test') {
            steps {
                // Pasos a Ejecutar
            }
        }
        stage('Deploy') {
            steps {
                // Pasos a Ejecutar
            }
        }
    }
}